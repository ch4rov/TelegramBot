# 🤖 Telegram Download Bot

Умный телеграм бот для скачивания видео, аудио и фото с популярных платформ. Полностью переписанный на модульной архитектуре с системой восстановления сообщений при крахе.

**Версия:** 2.0.1 | **Статус:** ✅ Production Ready

---

## ⚡ Основные возможности

### 📥 Поддерживаемые платформы
- **TikTok** - видео (3 стратегии обхода блокировок)
- **Instagram** - Reels
- **YouTube** - видео, Shorts, музыка
- **SoundCloud** - аудиотреки
- **Twitch** - клипы

### 🎯 Функции пользователя
- 📤 **Быстрая загрузка** - просто отправь ссылку
- 💾 **Умный кэш** - недавно загруженное хранится 3 минуты
- 🔄 **Очередь сообщений** - восстановление после крашей бота
- ⏳ **Плейсхолдер** - показывает индикатор загрузки во время обработки
- 📋 **Меню** - `/menu` с описанием команд

### 🔒 Админ-функции
- 🛡️ **Управление пользователями** - список, бан/разбан с причинами
- 📊 **Статус** - система работает, версия бота
- 💬 **Сообщения** - отправить сообщение пользователю
- ♻️ **Перезагрузка** - безопасный перезапуск с подтверждением
- 🔍 **Debug режим** - полное логирование всех действий
- 📜 **История** - сохранение всех логов и отправка в Discord

---

## 🚀 Быстрый старт

### Требования
- Python 3.10+
- FFmpeg (для конвертации видео)
- Telegram Bot Token
- (Опционально) Discord Webhook для уведомлений

### Установка

```bash
# Клонируем репо
git clone https://github.com/ch4rov/TelegramBot.git
cd TelegramBot

# Создаем виртуальное окружение
python -m venv venv
./venv/Scripts/activate  # Windows
source venv/bin/activate  # Linux/Mac

# Устанавливаем зависимости
pip install -r requirements.txt

# Скачиваем FFmpeg (опционально, если не установлен)
python install/install_ffmpeg.py
```

### Конфигурация

Создайте файл `.env`:

```env
BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_telegram_id
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...  # Опционально
```

### Запуск

```bash
# Через планировщик (автоматический рестарт при крахе)
python run.py

# Или прямой запуск
python main.py
```

---

## 📋 Команды бота

### Для всех пользователей
| Команда | Описание |
|---------|---------|
| `/start` | Приветствие и начало работы |
| `/menu` | Показать меню со всеми командами |
| Отправить ссылку | Скачать видео/аудио/фото |

### Для администратора
| Команда | Описание |
|---------|---------|
| `/status` | Статус системы и версия бота |
| `/users` | Список всех пользователей с статусами |
| `/ban [id] [reason]` | Заблокировать пользователя |
| `/unban [id]` | Разблокировать пользователя |
| `/answer [id] [text]` | Отправить сообщение пользователю |
| `/restart` | Перезагрузить бота (с подтверждением) |
| `/debug` | Включить/выключить debug режим |

---

## 🏗️ Архитектура

### Модульная структура

```
TelegramBot/
├── core/                    # 🔧 Переиспользуемые компоненты
│   ├── access_manager.py   # Управление доступом (админы, баны)
│   ├── media_sender.py     # Унифицированная отправка медиа
│   └── queue_manager.py    # Система восстановления сообщений
├── handlers/               # 📨 Обработчики Telegram событий
│   ├── message_handler.py  # Обработка сообщений с ссылками
│   ├── admin_handler.py    # Админ-команды
│   └── inline_handler.py   # Inline режим (@BotName url)
├── services/               # 🔌 Сервисы и утилиты
│   ├── logger_service.py   # Логирование и Discord вебхуки
│   ├── database_service.py # SQLite для пользователей и кэша
│   ├── cache_service.py    # Кэширование контента
│   └── downloads/          # Загрузчики для каждой платформы
├── logs/                   # 📝 Логи системы
│   └── files/
│       ├── full_log.txt        # Все логи
│       ├── other_messages.txt   # Прочие сообщения
│       └── debug.log           # Debug логи (при /debug)
├── main.py                 # Главный файл бота
├── run.py                  # Планировщик с автостартом
├── settings.py             # Конфигурация
├── messages.py             # Текстовые сообщения
├── VERSION                 # Номер версии (2.0.0)
└── CHANGELOG.md            # История изменений
```

### Ключевые компоненты

**AccessManager** - Унифицированное управление доступом
```python
- get_admin_id()           # ID админа из .env
- is_admin(user_id)        # Проверка прав админа
- check_user_access()      # Проверка бана пользователя
- ensure_user_access()     # Проверка + отправка ошибки
```

**MediaSender** - Унифицированная отправка медиа
```python
- send_media()             # Автоматическое определение типа
- _send_video()            # Видео с потоковой поддержкой
- _send_audio()            # Аудио с метаданными
- _send_photo()            # Фото
- get_file_id()            # Получить ID для кэша
```

**MessageQueueManager** - Восстановление при крахах
```python
- add_message()            # Сохранить сообщение в очередь
- get_pending_messages()   # Непроцессованные сообщения
- mark_as_processed()      # Отметить как обработанное
- recover_queued_messages() # Переобработать при старте
```

---

## 🔍 Debug режим

Для получения полной информации о работе бота:

1. **Включить debug:** отправь администратору `/debug`
   - Появятся логи в консоле
   - Создастся файл `logs/files/debug.log`

2. **Выключить debug:** отправь `/debug` еще раз

3. **Посмотреть логи:**
   ```bash
   tail -f logs/files/debug.log      # Linux/Mac
   Get-Content logs/files/debug.log -Tail 50  # Windows
   ```

---

## 📊 База данных

### Таблица users
```sql
user_id        INTEGER PRIMARY KEY
username       TEXT
first_seen     TEXT (ISO 8601)
last_seen      TEXT (ISO 8601)
is_banned      BOOLEAN
ban_reason     TEXT
is_active      BOOLEAN (бот не заблокирован в ЛС)
```

### Таблица file_cache
```sql
url            TEXT PRIMARY KEY
file_id        TEXT (Telegram file_id)
media_type     TEXT (video/audio/photo)
created_at     TEXT (ISO 8601)
title          TEXT (название файла)
```

---

## 🔄 Система восстановления сообщений

При крахе бота:
1. Сообщения пользователей сохраняются в `logs/files/message_queue.json`
2. При следующем старте бота:
   - Если был `/restart` - админ получит уведомление
   - Все очереженные сообщения переобработаны
   - Файл очищается

---

## 🔧 Конфигурация (settings.py)

```python
DOWNLOADS_DIR = "downloads"        # Папка для временных загрузок
DEBUG = False                      # Debug режим (вкл/выкл)
CACHE_TTL = 180                   # Время кэша в секундах
MAX_CONCURRENT_DOWNLOADS = 3      # Макс одновременных загрузок/юзер
```

---

## 📈 Производительность

- ⚡ **Быстрая загрузка** - средняя скорость 2-5 сек на видео
- 💾 **Умный кэш** - повторное воспроизведение за 1 сек
- 🔄 **Параллельная обработка** - до 3 загрузок одновременно на юзера
- 🗑️ **Автоочистка** - временные файлы удаляются автоматически

---

## 🤝 Контриьюция

Нашли баг или хотите добавить функцию? 
1. Fork репо
2. Создайте ветку (`git checkout -b feature/AmazingFeature`)
3. Коммитьте изменения (`git commit -m 'Add AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📝 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE)

---

## 🔗 Ссылки

- **Telegram:** [@ch4roff_bot](https://t.me/ch4roff_bot)
- **Разработчик:** [@ch4rov](https://t.me/ch4rov)
- **Репо:** [github.com/ch4rov/TelegramBot](https://github.com/ch4rov/TelegramBot)

---

## 📞 Поддержка

Если у тебя есть вопросы или проблемы:
- 📧 Напиши разработчику в Telegram
- 🐛 Откройте Issue на GitHub
- 💬 Обсудите в Discussion

---

**Последнее обновление:** 25.11.2025 (v2.0.0) ✨
