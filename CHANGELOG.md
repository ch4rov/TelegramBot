### 🚀 Глобальное обновление v2.3

**🔧 Архитектура и Ядро:**
* **Модульность:** Полный рефакторинг `services/downloads`. Теперь каждая платформа (`VK`, `YouTube`, `TikTok` и др.) имеет свою независимую стратегию в `services/platforms/`.
* **Local Bot API Server:** Добавлена поддержка локального сервера Telegram (Docker). Лимит загрузки увеличен с **50 МБ** до **2000 МБ**.
* **FFmpeg Auto-Install:** Бот теперь сам проверяет наличие FFmpeg и скачивает его при отсутствии.
* **Smart Cache:** Полностью удалено файловое кэширование. Теперь кэшируются только `file_id` в базе данных SQLite.

**✨ Новые функции:**
* **Cookies 2.0:** Поддержка загрузки `cookies.json` (из EditThisCookie) с автоматической конвертацией в Netscape формат.
* **YouTube Music:** Кнопка **"🎬 Загрузить клип"** при скачивании трека.
* **UX:** "Бегущий" статус загрузки (`upload_video` action) для больших файлов, чтобы клиент не думал, что бот завис.
* **Админка:**
    * Команда `/check` теперь проводит **реальный тест** (скачивание + отправка + удаление) по всем сервисам.
    * Команда `/status` с глубокой диагностикой (Ping, HDD, Tools).
    * Автоматическое обновление через `/update` (git pull + restart).

**🐛 Исправления:**
* **VK Video:** Исправлена загрузка HLS-потоков (серые видео) и добавлена поддержка приватных видео через куки.
* **TikTok:** Исправлен кодек видео (H.264) для воспроизведения на iPhone.
* **YouTube:** Исправлена проблема с отсутствием видеопотока (принудительная контейнеризация MP4).
* **Безопасность:** Улучшена валидация ссылок и защита от RCE.

**⚠️ Известные проблемы:**
* Модуль **TikTok Photo (Карусели)** временно отключен из-за изменений в API TikTok.